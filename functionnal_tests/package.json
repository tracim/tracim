{
  "name": "functionnal_tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "precypress-run": "npm run reset-db",
    "cypress-run": "node_modules/cypress/bin/cypress run",
    "precypress-open": "npm run reset-db",
    "cypress-open": "cd ../backend/ && . env/bin/activate && cd - && node_modules/cypress/bin/cypress open",
    "reset-db": "cd ../backend/ && . env/bin/activate && tracimcli db delete --force -c ./cypress_test.ini && tracimcli db init -c ./cypress_test.ini && cp ./tracim_cypress.sqlite ./tracim_cypress.sqlite.tmp && cd - ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.1.2"
  }
}
