import { htmlDocument } from './htmlDocument.js'
import { commentList, revisionList } from 'tracim_frontend_lib/dist/tracim_frontend_lib.testUtils.js'

const content = {
  htmlDocument,
  commentList,
  revisionList
}

export default content
