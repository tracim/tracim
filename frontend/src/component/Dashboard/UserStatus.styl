@import "~tracim_frontend_lib/src/css/Variable.styl"

.userstatus
  &__role
    display flex
    align-items center
    justify-content center
    margin-bottom 10px
    &__icon
      margin 0 5px
    &__text
      font-weight bold
  &__informations
    display flex
    text-align center
    font-size fontSizeSmall
    width 100%
    &__notification
      margin 5px auto 0
      cursor pointer
      & .btnswitch
        justify-content center
    &__requests
      margin auto
